{
    "$schema": "https://turbo.build/schema.json",
    "extends": ["//"],
    "tasks": {
        "build": {
            "dependsOn": ["codegen", "^build"],
            "outputs": ["dist/**"]
        }
    }
}
